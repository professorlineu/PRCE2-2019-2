/**********************************************************
- Autor:     Felipe Diogo
- Descrição: Laço de Repetição
**********************************************************/
#include <iostream>
#include <locale.h>
#include <cstdlib>

using namespace std;

int main()
{
    //Declaração de variáveis
    int ivalor1 = 0;
    int i = 0;
    int x = 0;
    int b = 0;
    int y = 0;
    int a = 0;
    int z = 0;
    int c = 0;
    int p = 0;
    int d = 0;
    int r = 0;
    int e = 0;
    int l = 0;
    int f = 0;
    int m = 0;
    int g = 0;
    int n = 0;
    int k = 0;
    int h = 0;
    int o = 0;
    int u = 0;

    //Configuração da tela de saída
    setlocale(LC_ALL,"");
    system("color F1");

    cout << "digite qualquer valor para que seja gerado a tabuada de 1 a 10 " << endl;
    cin >> ivalor1;

cout << "\nTabuada do 1 " << endl;

while (i != 11)
{

    x = (i * 1);

    cout << "\n" << 1 << "x" << i << "=" <<  x << endl;

    i++;
}
cout << "\nTabuada do 2 " << endl;

while (b != 11)
{

    y = (b * 2);

    cout << "\n" << 2 << "x" << b << "=" <<  y << endl;

    b++;
}
cout << "\nTabuada do 3 " << endl;

while (a != 11)
{

    z = (a * 3);

    cout << "\n" << 3 << "x" << a << "=" <<  z << endl;

    a++;
}
cout << "\nTabuada do 4 " << endl;

while (c != 11)
{

    p = (c * 4);

    cout << "\n" << 4 << "x" << c << "=" <<  p << endl;

    c++;
}
cout << "\nTabuada do 5 " << endl;

while (d != 11)
{

    r = (d * 5);

    cout << "\n" << 5 << "x" << d << "=" <<  r << endl;

    d++;
}
cout << "\nTabuada do 6 " << endl;

while (e != 11)
{

    l = (e * 6);

    cout << "\n" << 6 << "x" << e << "=" <<  l << endl;

    e++;
}
cout << "\nTabuada do 7 " << endl;

while (f != 11)
{

    m = (f * 7);

    cout << "\n" << 7 << "x" << f << "=" <<  m << endl;

    f++;
}
cout << "\nTabuada do 8 " << endl;

while (g != 11)
{

    n = (g * 8);

    cout << "\n" << 8 << "x" << g << "=" <<  n << endl;

    g++;
}
cout << "\nTabuada do 9 " << endl;

while (h != 11)
{

    k = (h * 9);

    cout << "\n" << 9 << "x" << h << "=" <<  k << endl;

    h++;
}
cout << "\nTabuada do 10 " << endl;

while (o != 11)
{

    u = (o * 10);

    cout << "\n" << 10 << "x" << o << "=" <<  u << endl;

    o++;
}








    return 0;
}
